{
  "license": "GPL-3.0",
  "scripts": {
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,scss,md}\"",
    "start": "tsc && node dist/app.js",
    "build": "tsc && pkg dist/app.js --out-path build"
  },
  "devDependencies": {
    "@types/node": "^20.1.1",
    "@types/prompt-sync": "^4.2.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "prompt-sync": "^4.2.0",
    "xlsx": "^0.18.5"
  }
}
